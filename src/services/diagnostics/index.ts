/**
 * Diagnostics — stub
 *
 * Implementação prevista na Fase 13 do roadmap nostr-filesync.md:
 * - Métricas locais (latência, upload success, etc.)
 * - Relay health score
 * - Repair tools (Check integrity, Rebuild index, Retry failed)
 * - Export diagnostics (sem dados sensíveis)
 */

export const DIAGNOSTICS_VERSION = '0.0.0-stub';
