/**
 * Device Registry — stub
 *
 * Implementação prevista na Fase 2 do roadmap nostr-filesync.md:
 * - Device entity: { id, name, platform, appVersion, lastSeen, capabilities }
 * - Descoberta de dispositivos via kind 30078 (NIP-78)
 * - Publicação/consulta de metadata
 * - UI em Settings → Devices
 */

export const DEVICE_REGISTRY_VERSION = '0.0.0-stub';
